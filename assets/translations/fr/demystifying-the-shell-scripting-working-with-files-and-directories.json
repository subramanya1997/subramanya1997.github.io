{
  "title": "Démystifier le scripting shell : travailler avec les fichiers et les répertoires",
  "excerpt": "Maîtrisez l'art de travailler avec les fichiers et les répertoires en scripting shell pour rationaliser vos tâches et améliorer votre efficacité. Apprenez à créer, copier, déplacer et supprimer des fichiers et des répertoires, ainsi qu'à lire et écrire dans des fichiers à l'aide d'exemples pratiques. Découvrez la puissance de la recherche de fichiers et de répertoires avec la commande `find`. Améliorez vos compétences en scripting shell grâce à des ressources et des tutoriels précieux, et exploitez pleinement le potentiel de la gestion des fichiers et des répertoires dans le shell.",
  "content_html": "<p>Dans mes articles de blog précédents, nous avons abordé les bases de l'utilisation du shell, présenté le scripting shell pour les débutants et exploré des techniques avancées et les meilleures pratiques. Dans cet article, nous allons nous concentrer sur le travail avec les fichiers et les répertoires dans les scripts shell. Nous discuterons des tâches courantes telles que la création, la copie, le déplacement et la suppression de fichiers et de répertoires, ainsi que la lecture et l'écriture dans des fichiers. Nous fournirons également quelques ressources pour approfondir vos connaissances.</p>\n<h2>Créer des fichiers et des répertoires</h2>\n<p>Pour créer un nouveau fichier dans un script shell, vous pouvez utiliser la commande <code>touch</code> :</p>\n<pre><code class=\"language-bash\">touch new_file.txt\n</code></pre>\n<p>Pour créer un nouveau répertoire, vous pouvez utiliser la commande <code>mkdir</code> :</p>\n<pre><code class=\"language-bash\">mkdir new_directory\n</code></pre>\n<h2>Copier et déplacer des fichiers et des répertoires</h2>\n<p>Pour copier un fichier, vous pouvez utiliser la commande <code>cp</code> :</p>\n<pre><code class=\"language-bash\">cp source_file.txt destination_file.txt\n</code></pre>\n<p>Pour copier un répertoire, vous pouvez utiliser l'option <code>-r</code> (récursive) :</p>\n<pre><code class=\"language-bash\">cp -r source_directory destination_directory\n</code></pre>\n<p>Pour déplacer un fichier ou un répertoire, vous pouvez utiliser la commande <code>mv</code> :</p>\n<pre><code class=\"language-bash\">mv source_file.txt destination_file.txt\n</code></pre>\n<h2>Supprimer des fichiers et des répertoires</h2>\n<p>Pour supprimer un fichier, vous pouvez utiliser la commande <code>rm</code> :</p>\n<pre><code class=\"language-bash\">rm file_to_delete.txt\n</code></pre>\n<p>Pour supprimer un répertoire, vous pouvez utiliser l'option <code>-r</code> (récursive) :</p>\n<pre><code class=\"language-bash\">rm -r directory_to_delete\n</code></pre>\n<h2>Lire et écrire dans des fichiers</h2>\n<p>Pour lire le contenu d'un fichier, vous pouvez utiliser la commande <code>cat</code> :</p>\n<pre><code class=\"language-bash\">cat file_to_read.txt\n</code></pre>\n<p>Pour écrire dans un fichier, vous pouvez utiliser l'opérateur <code>&gt;</code> pour écraser le fichier ou l'opérateur <code>&gt;&gt;</code> pour ajouter à la fin du fichier :</p>\n<pre><code class=\"language-bash\">echo \"This is a new line\" &gt; file_to_write.txt\necho \"This is another new line\" &gt;&gt; file_to_write.txt\n</code></pre>\n<p>Pour lire un fichier ligne par ligne, vous pouvez utiliser une boucle <code>while</code> avec la commande <code>read</code> :</p>\n<pre><code class=\"language-bash\">#!/bin/bash\n\nwhile IFS= read -r line; do\n  echo \"Line: $line\"\ndone &lt; file_to_read.txt\n</code></pre>\n<h2>Rechercher des fichiers et des répertoires</h2>\n<p>Pour rechercher des fichiers et des répertoires, vous pouvez utiliser la commande <code>find</code> :</p>\n<pre><code class=\"language-bash\">find /path/to/search -name \"file_pattern\"\n</code></pre>\n<p>Par exemple, pour trouver tous les fichiers <code>.txt</code> dans le répertoire <code>/home/user</code>, vous pouvez utiliser :</p>\n<pre><code class=\"language-bash\">find /home/user -name \"*.txt\"\n</code></pre>\n<h2>Ressources</h2>\n<p>Pour améliorer davantage vos compétences dans le travail avec les fichiers et les répertoires en scripts shell, voici quelques ressources :</p>\n<ul>\n<li><a href=\"https://www.tecmint.com/linux-file-management-commands/\">File Management Commands in Linux</a> : Un guide complet des commandes de gestion de fichiers sous Linux.</li>\n<li><a href=\"https://www.howtogeek.com/112674/how-to-find-files-and-folders-in-linux-using-the-command-line/\">Linux Find Command Examples</a> : Une collection d'exemples d'utilisation de la commande <code>find</code> sous Linux.</li>\n</ul>\n<p>En conclusion, travailler avec les fichiers et les répertoires est un aspect essentiel du scripting shell. En maîtrisant les tâches courantes telles que la création, la copie, le déplacement et la suppression de fichiers et de répertoires, ainsi que la lecture et l'écriture dans des fichiers, vous serez bien équipé pour gérer une large gamme de tâches de scripting shell.</p>",
  "source_hash": "sha256:238fb0867e740157e3b109f5d25321f48db87d404c871f1eda7b1082b81e89b3",
  "model": "moonshotai/kimi-k2.6",
  "generated_at": "2026-06-10T20:20:58.599809+00:00"
}